int func fi(int x, int y)
{
 	int z;
 z= x*(x+y);
 return z; 
};

str func fs (str a, str b)
{
 str c;
 str func substr(str a, int p, int n);
 c = substr(a, 1, 3)+ b;
 return c; 
};  

main
{
 int x;
 int y;
 int z;
 str sa;
 str sb;
 str sc;
 int func strlen(string p);
 x = 1;
 y = 5;
 sa = "1234567890";
 sb = "1234567890"; 
 z = fi(x,y);
 sc = fs(sa,sb);
 cprint "контрольный пример";
 cprint z;
 cprint sc;
 cprint strlen(sc);
 return 0;
};
