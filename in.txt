global int indexator = 0;

str func concat(str a, str b)
{
 return a + b;
}

int func fi(int x, int y)
{
 int z;
 z = x * (x + y);
 return z;
}

main
{
 int x;
 int y;
 int z;
 str sa;
 str sb;
 str sc;
 x = 1;
 y = 5;

if(x<y)
{
 int p;
}

 sa = "1234567890";
 sb = "1234567890";
 z = fi(x,y);
 sc = concat(sa,sb);
 cprint "контрольный пример";
 cprint z;
 cprint sc;
};